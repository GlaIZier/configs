#!/bin/sh
#   ~/.profile
#
# executed after .bash_profile and .bash_login if they are not specified and it is a login shell (ex. login from tty or ssh or GUI login?) and --noprofile is not specified
# Common config for all shells like LESS, PATH, MANPATH, LC_*


# env variables
# export JAVA_HOME="/usr/lib/jvm/java-8-oracle"
# export PATH="/usr/bin:/bin:/usr/sbin:/sbin"
# export PATH="/usr/local/bin:/usr/local/sbin:$PATH"

#THIS MUST BE AT THE END OF THE FILE FOR SDKMAN TO WORK!!!
# export SDKMAN_DIR="/home/GlaIZier/.sdkman"
# [[ -s "/home/GlaIZier/.sdkman/bin/sdkman-init.sh" ]] && source "/home/GlaIZier/.sdkman/bin/sdkman-init.sh"
# export SDKMAN_OFFLINE_MODE=false